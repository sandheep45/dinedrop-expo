import React from "react";
import { Text } from "react-native";
import SafeArea from "../components/global/SafeArea";

const Onboarding = () => {
  return (
    <SafeArea>
      <Text className="dark:text-white">Onboarding</Text>
    </SafeArea>
  );
};

export default Onboarding;
